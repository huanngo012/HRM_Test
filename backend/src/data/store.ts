import { IEmployee } from "../types/employee.type";
import { ILeaveRequest } from "../types/leave-request.type";

let employees: IEmployee[] = [];
let leaveRequests: ILeaveRequest[] = [];

export { employees, leaveRequests };
